import { Component } from '@angular/core';
import { Badge } from '../badge/badge';

@Component({
  selector: 'app-card',
  imports: [Badge],
  templateUrl: './card.html',
  styleUrl: './card.css'
})
export class Card {

}
